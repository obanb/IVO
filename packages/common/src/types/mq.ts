export type Queues = {
    personalisation: {
        jobData: {
            databaseId: string;
            requestId: string;
            data: unknown;
            datatype: string;
        };
    };
};
